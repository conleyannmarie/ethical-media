<div>
    {{#each this}}
    <div class="row">
        <div class="col-md-12">
            <h2>{{username}}</h2>
            <h2>{{overall}}</h2>
            {{#categories}}
            <p>{{name}}</p>
            {{#each ratings}}
            <p>{{rating}}</p>
            <p>{{about_rating}}</p>
            {{/each}}
            {{/categories}}
        </div>
    </div>
    {{/each}}
</div>

<script src="/javascript/script.js"></script>